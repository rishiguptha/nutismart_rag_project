# You can define non-sensitive, shared configuration constants here if needed.
# For now, most config is handled via environment variables or directly in scripts.

# Example:
# DEFAULT_TOP_K_RETRIEVAL = 3